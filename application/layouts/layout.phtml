<!doctype html>
<html lang="fr" >
    <head>
        <style>body { padding-top: 80px; }</style>
        <?php echo $this->render("includes/header.phtml"); ?>
    </head>

    <body>
        <?php echo $this->render("includes/nav-main.phtml"); ?>

        <?php
        if (isset($GLOBALS['kernel'])) {
            try {
                $bannerReq = \Symfony\Component\HttpFoundation\Request::create(
                    '/changelog/banner', 'GET', [], $_COOKIE, [], $_SERVER
                );
                $bannerRes = $GLOBALS['kernel']->handle(
                    $bannerReq,
                    \Symfony\Component\HttpKernel\HttpKernelInterface::SUB_REQUEST
                );
                echo $bannerRes->getContent();
            } catch (\Exception $e) { /* silent fail */ }
        }
        ?>
        <?php echo $this->placeholder('lateral_navigation'); ?>

        <div class='container-fluid main-container-f' >
            <?php echo $this->render("includes/notifications.phtml"); ?>
            <?php echo $this->placeholder('before_content') ?>
            <?php echo $this->layout()->content; ?>
            <?php echo $this->placeholder('after_content') ?>
        </div>

        <?php echo $this->render("includes/footer.phtml"); ?>
    </body>
</html>
